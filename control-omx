#!/bin/bash

while [ 1 ]
do
	read state
	echo "state: $state"
	# movement
	if [[ $state == '1;' ]]
	then
		echo "go" > omx-go
	# no movement
	elif [[ $state == '0;' ]]
	then
		echo "stop" > omx-go
		pid=$(pgrep omxplayer)
		kill $pid
	fi
done
